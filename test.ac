// 这是一个测试程序
main : int () 
    int x, y;
    x = 10;
    y = 20;
    
    if (x < y) {
        outputInt(x);
    } else {
        outputInt(y);
    }
    
    while (x > 0) {
        x = x - 1;
        if (x == 5) {
            continue;
        }
        outputInt(x);
    }
}